export const fbPrismaMock = {
  facebookEvent: {
    groupBy: jest.fn(),
    aggregate: jest.fn(),
    findMany: jest.fn(),
  },
};

export const ttkPrismaMock = {
  tiktokEvent: {
    groupBy: jest.fn(),
    aggregate: jest.fn(),
    findMany: jest.fn(),
  },
};
